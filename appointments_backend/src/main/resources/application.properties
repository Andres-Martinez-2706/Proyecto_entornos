spring.application.name=appointments
server.port=8080

# =====================
# BASE DE DATOS
# =====================
spring.datasource.url=jdbc:postgresql://ep-bold-brook-add31moy-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_xW6yEcD1zmOi
spring.jpa.hibernate.ddl-auto=none  
spring.jpa.show-sql=false            
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# =====================
# JWT
# =====================
jwt.secret=mi_clave_secreta_para_el_proyecto_de_citas
jwt.expiration= 3600000

# =====================
# EMAIL (SMTP Gmail)
# =====================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=tests.programacion.afmo@gmail.com
spring.mail.password=${SPRING_MAIL_PASSWORD} 
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
# Logs para emails (descomentar para debugging)
# logging.level.org.springframework.mail=DEBUG
# logging.level.uis.edu.co.appointments.service.EmailService=DEBUG

# =====================
# CORS y LOGGING
# =====================
cors.allowed.origins=*
logging.level.root=INFO
logging.level.uis.edu.co.appointments=DEBUG
logging.pattern.console=%d{HH:mm:ss} [%level] %msg%n

# =====================
# SCHEDULING (TAREAS PROGRAMADAS)
# =====================
# Pool de threads para tareas programadas
spring.task.scheduling.pool.size=3

# Intervalo de verificación de notificaciones (en milisegundos)
# 600000 = 10 minutos
scheduler.notification.check.interval=600000

# Intervalo de auto-completado de citas (en milisegundos)
# 3600000 = 1 hora
scheduler.appointment.complete.interval=3600000